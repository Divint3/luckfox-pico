## 2026-09-25 Luckfox Pico CI Workflow
- 环境曾出现文件被暂存删除后又恢复的情况；最终完整 SDK 已检出（README、build.sh、media、project、sysdrv、tools）。
- 摸清构建体系：根目录 build.sh → project/build.sh；板级选择 = 符号链接 .BoardConfig.mk → project/cfg/BoardConfig_IPC/<配置>.mk（等价于 ./build.sh lunch，可非交互化）；全量编译入口 ./build.sh allsave；产物在 output/image。
- 交叉工具链在 tools/linux/toolchain/arm-rockchip830-linux-uclibcgnueabihf，source env_install_toolchain.sh 设置环境。
- 已创建 .github/workflows/build.yml：ubuntu-22.04、释放磁盘、装依赖、缓存 buildroot dl、非交互选板、allsave 编译、上传固件 artifact，workflow_dispatch 可选板级。
